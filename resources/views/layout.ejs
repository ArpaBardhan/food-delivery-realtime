<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- CSS only -->
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" type="text/css" href="https://unpkg.com/notie/dist/notie.min.css">
    <link rel="stylesheet" href="https://maxst.icons8.com/vue-static/landings/line-awesome/line-awesome/1.3.0/css/line-awesome.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Lato:wght@300;400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheets/app.css">
    <title>DriveAway Delivery System</title>
</head>

<body>
  <nav class="container mx-auto flex items-center justify-between py-4">
    <div>
      <a href="/"><img src="/images/logo.png" alt="logo"></a>
    </div>
    <div>
      <ul class="flex items-center">
        
        <% if(user) { %>
          <% if(user.role!=='customer'){ %>
            <!-- <li class="ml-6"><a href="#">Menu</a></li> -->
          <% } else { %>
          <li class="ml-6"><a href="/">Home</a></li>
          <% } %>
          <li class="ml-6"><a href="/<%- user.role %>/orders">Order</a></li>
          <li class="ml-6">
            <form id="logout" action="/logout" method="POST">
              <a onclick="document.getElementById('logout').submit()" href="#">Logout</a></li>
            </form>
            
        <% } else { %>
          <li class="ml-6"><a href="/">Home</a></li>
          <li class="ml-6"><a href="/register">Register</a></li>
          <li class="ml-6"><a href="/login">Login</a></li>
        <% } %>
        
        <li class="ml-6 "><a href="/cart" class="inline-block px-4 py-2 rounded-full flex items-center">
          <span id='cartCounter' class="text-white font bold pr-2"><%= session.cart ? session.cart.totalQty : '' %></span>
          <img src="/images/cart.png" alt="">
        </a></li>
      </ul>
    </div>
  </nav>
    <%- body %>
        <footer>
            <div class="flex items-center justify-center my-4">

                <p class="text-gray-500 text-xs">All rights reserved &copy;2021 Food Hut Orgs.</p>
            </div>
        </footer>

        <script src="https://unpkg.com/notie"></script>
        <script src='/socket.io/socket.io.js'></script>
        <script src="/javascripts/app.js"></script>
</body>

</html>